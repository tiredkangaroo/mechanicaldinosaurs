name: Build and Release Daemon, Automation Engine, and VM Proxy

on:
  push:
    paths:
      - "**.go"
      - "go.mod"
      - "go.sum"
      - "daemon/**"
      - "automation-engine/**"
      - "proxy/**"
      - ".github/workflows/release.yml"

permissions:
  contents: write

jobs:
  build:
    name: Build Daemon for multiple OS/ARCH
    runs-on: ubuntu-latest

    strategy:
      matrix:
        goos: [linux]
        goarch: [amd64, arm64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Install libvirt dependencies (amd64)
        if: matrix.goarch == 'amd64'
        run: |
          sudo apt-get update
          sudo apt-get install -y libvirt-dev

      - name: Install libvirt cross-compile dependencies (arm64)
        if: matrix.goarch == 'arm64'
        run: |
          sudo dpkg --add-architecture arm64

          # 1. Restrict the default Ubuntu repositories to ONLY pull amd64
          if [ -f /etc/apt/sources.list.d/ubuntu.sources ]; then
            sudo sed -i '/^Types: deb/a Architectures: amd64' /etc/apt/sources.list.d/ubuntu.sources
          fi

          # 2. Add the arm64 specific ports repository using the new DEB822 format
          sudo tee /etc/apt/sources.list.d/arm64-ports.sources <<EOF
          Types: deb
          URIs: http://ports.ubuntu.com/ubuntu-ports/
          Suites: noble noble-updates noble-security
          Components: main restricted universe multiverse
          Architectures: arm64
          Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
          EOF

          # 3. Update and Install
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu libvirt-dev:arm64

      - name: Build and zip for ${{ matrix.goos }}/${{ matrix.goarch }}
        run: |
          # Define paths relative to the project root workspace
          WORKSPACE_ROOT=$(pwd)
          build_dir="${WORKSPACE_ROOT}/builds/md-daemon-${{ matrix.goos }}-${{ matrix.goarch }}"
          output_name="md-daemon"
          renamed_binary="md-daemon-${{ matrix.goos }}-${{ matrix.goarch }}"

          mkdir -p "$build_dir"

          echo "Building for ${{ matrix.goos }}/${{ matrix.goarch }}..."

          # Handle cross-compilation environment flags
          if [ "${{ matrix.goarch }}" = "arm64" ]; then
            export CGO_ENABLED=1
            export CC=aarch64-linux-gnu-gcc
            export PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig:/usr/share/pkgconfig
            export PKG_CONFIG_ALLOW_CROSS=1
          fi

          # Move to daemon folder to build, but output back to absolute build_dir path
          cd daemon
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o "${build_dir}/${output_name}" .

          # Move back to build directory to rename and zip safely
          cd "$build_dir"
          chmod +x "$output_name"
          mv "$output_name" "$renamed_binary"

          zip_name="md-daemon-${{ matrix.goos }}-${{ matrix.goarch }}.zip"
          zip "$zip_name" "$renamed_binary"

      - name: Upload zipped build
        uses: actions/upload-artifact@v4
        with:
          name: md-daemon-${{ matrix.goos }}-${{ matrix.goarch }}
          path: builds/md-daemon-${{ matrix.goos }}-${{ matrix.goarch }}/*.zip

  build-simple:
    name: Build ${{ matrix.project }} for multiple OS/ARCH
    runs-on: ubuntu-latest

    strategy:
      matrix:
        project: [automation-engine, proxy]
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Build and zip ${{ matrix.project }} for ${{ matrix.goos }}/${{ matrix.goarch }}
        run: |
          WORKSPACE_ROOT=$(pwd)
          project="${{ matrix.project }}"
          build_dir="${WORKSPACE_ROOT}/builds/${project}-${{ matrix.goos }}-${{ matrix.goarch }}"
          output_name="${project}"
          renamed_binary="${project}-${{ matrix.goos }}-${{ matrix.goarch }}"

          # Windows binaries need an .exe suffix
          if [ "${{ matrix.goos }}" = "windows" ]; then
            output_name="${output_name}.exe"
            renamed_binary="${renamed_binary}.exe"
          fi

          mkdir -p "$build_dir"

          echo "Building ${project} for ${{ matrix.goos }}/${{ matrix.goarch }}..."

          cd "$project"
          CGO_ENABLED=0 GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o "${build_dir}/${output_name}" .

          cd "$build_dir"
          mv "$output_name" "$renamed_binary"
          if [ "${{ matrix.goos }}" != "windows" ]; then
            chmod +x "$renamed_binary"
          fi

          zip_name="${project}-${{ matrix.goos }}-${{ matrix.goarch }}.zip"
          zip "$zip_name" "$renamed_binary"

      - name: Upload zipped build
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.project }}-${{ matrix.goos }}-${{ matrix.goarch }}
          path: builds/${{ matrix.project }}-${{ matrix.goos }}-${{ matrix.goarch }}/*.zip

  release:
    name: Create 'latest' GitHub Release
    runs-on: ubuntu-latest
    needs: [build, build-simple]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all zipped artifacts
        uses: actions/download-artifact@v4
        with:
          path: final-builds
          merge-multiple: true

      - name: Create or update 'latest' release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Use 'gh' directly (pre-installed on ubuntu-latest)
          if gh release view latest &>/dev/null; then
            echo "Release 'latest' exists. Deleting and recreating..."
            gh release delete latest --yes
          fi

          gh release create latest \
            --title "latest build" \
            --notes "Automated build for latest commit" \
            --latest

      - name: Upload all zips to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          find final-builds -type f -name "*.zip" -exec gh release upload latest {} --clobber \;
